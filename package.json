{
  "name": "commercial",
  "private": true,
  "scripts": {
    "dev:cms": "node scripts/dev-cms.mjs",
    "build:cms": "npm run build --workspace @commercial/cms",
    "lint:cms": "npm run lint --workspace @commercial/cms"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
